function submitForm() {
    var name = document.getElementById('name').value;
    var name = document.getElementById('email').value;
    var name = document.getElementById('message').value;

    alert(name + ' ' + email + ' ' + mensagem);
}

